rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Batch PDFs - users can only access their own uploaded PDFs
    match /batch-pdfs/{userId}/{filename} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
